# Backlog

Everything known to be outstanding, ranked by what unblocks a host other than the first
one. Each item stands alone: a fresh session picks one up without any conversation.

Items are removed when done, not marked done, and their line leaves this file in the
same change. The [log](../log.md) records what left.

## Ranked

1. [The served surface is a fraction of the library, and nothing says so](the-served-surface-is-a-fraction-of-the-library.md) - 17 tools over the wire against 269 published names, so a wire-only host cannot bind a parameter, build a step tree, count a plan, run a control test or search the index.
2. [The WDK server proves admission in another repository, at an older conformance pin](the-wdk-server-proves-admission-in-another-repository.md) - this CI admits the research server on 8110 only, and the 8100 lane needs the consuming application's database.
3. [An absent record type defaults to the first host's gene type](an-absent-record-type-defaults-to-the-first-hosts-gene-type.md) - a step read silently asks for three gene attributes when the caller says nothing.
4. [The settings still answer to the first host's variable names](the-settings-still-answer-to-the-first-hosts-variable-names.md) - two alias choices name an application no other host deploys, and no deployment sets either.
5. [api_debug names a host API in the index settings, and collides with one](api-debug-names-a-host-api-in-the-index-settings.md) - one field name shared with the assistant runtime, so one flag drives two engines.
6. [The research process imports the WDK credential verifier it never uses](the-research-process-imports-the-wdk-credential-verifier.md) - reading the version pulls the OAuth subject cache into a process with no account.
7. [The vocabulary parameter codec is a second answer to the client's question](the-vocabulary-parameter-codec-is-a-second-answer-to-the-clients-question.md) - the two repositories disagree about the wire form of a single-pick value.
8. [The phyletic parameter names are declared twice, in two repositories](the-phyletic-parameter-names-are-declared-twice.md) - one WDK fact, two declarations, one checker that can only anchor one.
9. [The step tree fold and the record type match are client shapes](the-step-tree-fold-and-the-record-type-match-are-client-shapes.md) - two pure functions over the client's own types, living in a tool server.
